apply plugin: 'idea'

subprojects{
    apply plugin: 'java'
    apply plugin: 'idea'

    repositories{
        jcenter()
        mavenLocal()
    }

    dependencies {
        testCompile group: 'com.codeborne', name: 'selenide', version: '3.7'
        testCompile project(':core')
    }
}
