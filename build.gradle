apply plugin: 'idea'

subprojects{
    apply plugin: 'java'
    apply plugin: 'idea'

    repositories{
        jcenter()
    }

    dependencies {
        testCompile group: 'com.codeborne', name: 'selenide', version: '3.6'
        testCompile project(':core')
    }
}
