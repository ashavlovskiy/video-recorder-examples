apply plugin: 'idea'

subprojects{
    apply plugin: 'java'
    apply plugin: 'idea'

    repositories{
        mavenLocal()
        jcenter()
    }

    dependencies {
        testCompile group: 'com.codeborne', name: 'selenide', version: '3.7'
        testCompile project(':core')
    }
}
